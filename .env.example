# Copy to .env (not committed) and fill in for local development.
# The app reads this from the environment — see README "Configuration".
ANTHROPIC_API_KEY=sk-ant-...

# Optional: override the default model (see src/askmycity/agent.py)
# ASKMYCITY_MODEL=claude-sonnet-5
